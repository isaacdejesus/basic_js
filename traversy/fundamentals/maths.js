const num1 = 100;
const num2 = 50;
let val;


//basic math ops
val = num1 + num2;
val = num1 * num2;
val = num1 % num2;

//Math Objects
val = Math.PI;
val = Math.round(2.3);
val = Math.ceil( 2.4);      //round up
val = Math.floor(2.8);      //round down
val = Math.sqrt(6);
val = Math.abs(-3);
val = Math.pow(8,4);
val = Math.min(2, 44, 55, 3, 4);
val = Math.random();
val = Math.floor(Math.random() * 100 + 1);      //produces random number between 1-100

